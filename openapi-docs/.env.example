# Backend base URL this app fetches /openapi.json from, server-side, on
# every request (app/route.ts). Never read client-side.
#
# Local dev: the backend running on your machine.
# docker-compose: the compose network's service name, e.g. http://api:8000.
API_URL=http://localhost:8000
